# 代码优化记录

## 本次优化内容

### 1. 客户端补全为可控核心

- 重构 `client/client.c`，补充本地 TCP 控制服务，默认监听 `127.0.0.1:9000`。
- 支持以下控制命令：
  - `LIST`
  - `PLAY <id>`
  - `STOP`
  - `QUIT`
- 保留命令行运行能力，并新增参数：
  - `-g` 指定组播地址
  - `-p` 指定 UDP 端口
  - `-t` 指定 TCP 控制端口
  - `-P` 指定播放器程序
  - `-c` 启动后直接播放指定频道
  - `-i` 首次收到节目单后进入交互式选台
  - `-n` 关闭 TCP 控制服务

### 2. 客户端播放链路规范化

- 保留 UDP 组播收流、jitter buffer、有序排水和本地播放器解码流程。
- 切台时会重置 jitter buffer 和输出缓冲，并重建播放器子进程，避免串台和状态污染。
- 修正 `mpg123` 参数顺序，避免播放器误把 `-q` 当文件名。
- 增加播放器快速退出检测，避免 TCP 返回“播放成功”但子进程实际上启动失败。

### 3. 服务端启动逻辑修复

- 修复 `meduim/server.c` 中错误的绝对路径硬编码。
- 服务端现在会按以下顺序寻找媒体目录：
  - 启动参数传入的目录
  - `./meduim_data`
  - `./meduim/meduim_data`

### 4. 令牌桶限速修正

- 保留 `meduim/token.c` 现有的 `100ms` 补令牌节奏。
- 原因是服务端当前 `token_init(12288, 1600)` 这组参数就是按这个节奏调出来的。
- 如果直接改成 `1s` 补充而不重算速率参数，服务端实际推流速率会明显下降，客户端会出现卡顿和断续播放。

## 当前运行方式

### 服务端

```bash
cd meduim
./server
```

如需后台运行：

```bash
cd meduim
./server -d
```

如需显式指定媒体目录：

```bash
cd meduim
./server ./meduim_data
```

### 客户端核心

```bash
cd client
./clinet
```

默认会：

- 加入组播 `224.1.1.1:8888`
- 启动本地 TCP 控制口 `127.0.0.1:9000`
- 等待 Qt 控制端或其他 TCP 客户端发命令

### Qt 控制端

Qt 程序保持原有用法，默认连接：

- IP: `127.0.0.1`
- Port: `9000`

## 本次验证结果

- `meduim/server` 编译通过。
- `client/clinet` 编译通过。
- 已本地验证 TCP 控制命令：
  - `LIST`
  - `PLAY 1`
  - `STOP`
  - `QUIT`
