# 代码优化记录

## 本次优化内容

### 1. 客户端补全为可控核心

- 重构 `client/client.c`，补充本地 TCP 控制服务，默认监听 `127.0.0.1:9000`。
- 支持以下控制命令：
  - `LIST`
  - `PLAY <id>`
  - `STOP`
  - `QUIT`
- 保留命令行运行能力，并新增参数：
  - `-g` 指定组播地址
  - `-p` 指定 UDP 端口
  - `-t` 指定 TCP 控制端口
  - `-P` 指定播放器程序
  - `-c` 启动后直接播放指定频道
  - `-i` 首次收到节目单后进入交互式选台
  - `-n` 关闭 TCP 控制服务

### 2. 客户端播放链路规范化

- 保留 UDP 组播收流、jitter buffer、有序排水和本地播放器解码流程。
- 切台时会重置 jitter buffer 和输出缓冲，并重建播放器子进程，避免串台和状态污染。
- 修正 `mpg123` 参数顺序，避免播放器误把 `-q` 当文件名。
- 增加播放器快速退出检测，避免 TCP 返回“播放成功”但子进程实际上启动失败。

### 3. 服务端启动逻辑修复

- 修复 `meduim/server.c` 中错误的绝对路径硬编码。
- 服务端现在会按以下顺序寻找媒体目录：
  - 启动参数传入的目录
  - `./meduim_data`
  - `./meduim/meduim_data`

### 4. 令牌桶限速修正

- 保留 `meduim/token.c` 现有的 `100ms` 补令牌节奏。
- 原因是服务端当前 `token_init(12288, 1600)` 这组参数就是按这个节奏调出来的。
- 如果直接改成 `1s` 补充而不重算速率参数，服务端实际推流速率会明显下降，客户端会出现卡顿和断续播放。

## 当前运行方式

### 服务端

```bash
cd meduim
./server
```

如需后台运行：

```bash
cd meduim
./server -d
```

如需显式指定媒体目录：

```bash
cd meduim
./server ./meduim_data
```

### 客户端核心

```bash
cd client
./clinet
```

默认会：

- 加入组播 `224.1.1.1:8888`
- 启动本地 TCP 控制口 `127.0.0.1:9000`
- 等待 Qt 控制端或其他 TCP 客户端发命令

### Qt 控制端

Qt 程序保持原有用法，默认连接：

- IP: `127.0.0.1`
- Port: `9000`

## 本次验证结果

- `meduim/server` 编译通过。
- `client/clinet` 编译通过。
- 已本地验证 TCP 控制命令：
  - `LIST`
  - `PLAY 1`
  - `STOP`
  - `QUIT`

## 补充规范化

- 将客户端中的默认控制端口、监听 backlog、epoll 超时、播放器启动等待、子进程轮询间隔等魔法数字抽成宏。
- 将服务端中的守护进程参数、令牌桶参数、节目单广播周期抽成宏。
- 将媒体扫描与发包模块中的文件类型值、glob 缓冲区大小、保留字节偏移、重试等待时间抽成宏。
- 将 Qt 控制端中的默认 IP、默认端口、窗口大小、断开等待时间抽成宏，便于和客户端默认配置保持一致。

---

## 嵌入式 Linux 优化特性

### 5. 统一配置文件系统

**实现位置**: `common/config.c` 和 `common/config.h`

**功能特性**:
- 支持 INI 格式配置文件解析（`key=value` 格式）
- 支持注释行（`#` 开头）
- 自动去除首尾空白字符
- 提供类型安全的读取接口：
  - `config_get_string()` - 字符串
  - `config_get_int()` - 整数
  - `config_get_uint16()` - 无符号16位整数
  - `config_get_bool()` - 布尔值
- 链表存储配置项，支持动态加载和释放

**集成情况**:
- 服务端：`server.conf` 配置文件，支持媒体目录、组播地址、令牌桶参数、守护进程模式等
- 客户端：`client.conf` 配置文件，支持组播地址、控制端口、播放器路径、缓冲区大小、心跳参数、资源监控、看门狗等
- 配置优先级：命令行参数 > 配置文件 > 默认值

### 6. 统一日志系统

**实现位置**: `common/logger.c` 和 `common/logger.h`

**功能特性**:
- 四级日志：DEBUG / INFO / WARN / ERROR
- 支持输出到文件或 stdout
- 线程安全的日志写入
- 格式化输出：`[时间戳] [级别] [模块名] 消息内容`
- 宏接口简化调用：`LOG_DEBUG()`, `LOG_INFO()`, `LOG_WARN()`, `LOG_ERROR()`

**集成情况**:
- 服务端：替换所有 `fprintf` 和 `perror` 为统一日志调用
- 客户端：替换所有 `printf` 和 `fprintf` 为统一日志调用
- 支持守护进程模式下的日志文件输出

### 7. 心跳/重连机制

**实现位置**: `common/heartbeat.c` 和 `common/heartbeat.h`

**功能特性**:
- 数据包超时检测：监控最后接收时间，超时自动触发重连
- 组播组自动重连：调用 `rejoin_multicast()` 重新加入组播组
- 播放器健康检查：定期检测播放器进程是否存活
- 重连计数和空闲时间统计：便于故障诊断
- 可配置参数：
  - `heartbeat_timeout` - 数据包超时时间（秒）
  - `heartbeat_check_interval` - 检查间隔（秒）
  - `max_reconnect_attempts` - 最大重连次数

**集成情况**:
- 客户端主循环集成心跳检查逻辑
- 接收到数据包时自动更新心跳时间戳
- 超时后自动重连组播组并重置播放器

### 8. 资源监控与自适应

**实现位置**: `common/resource_monitor.c` 和 `common/resource_monitor.h`

**功能特性**:
- 实时监控系统资源：
  - CPU 使用率（通过 `/proc/stat` 计算）
  - 内存使用率（通过 `/proc/meminfo` 读取）
  - 网络流量（通过 `/proc/net/dev` 统计）
- 四级资源等级评估：CRITICAL / LOW / NORMAL / ABUNDANT
- 自适应策略：
  - 根据 CPU 和内存使用率动态调整缓冲区大小
  - 根据资源状态建议比特率调整
- 可配置参数：
  - `resource_monitor_enabled` - 启用/禁用监控
  - `resource_monitor_interval` - 监控间隔（秒）
  - `cpu_threshold_high` / `cpu_threshold_low` - CPU 阈值
  - `mem_threshold_high` / `mem_threshold_low` - 内存阈值

**集成情况**:
- 客户端主循环定期更新资源状态
- 根据资源等级动态调整系统行为
- 资源紧张时自动降低缓冲区大小和比特率

### 9. 看门狗集成

**实现位置**: `common/watchdog.c` 和 `common/watchdog.h`

**功能特性**:
- 支持 Linux 硬件看门狗设备（`/dev/watchdog`）
- 定期喂狗机制，防止系统死锁
- 魔术关闭支持，正常退出时安全关闭看门狗
- 可配置参数：
  - `watchdog_enabled` - 启用/禁用看门狗（默认禁用）
  - `watchdog_device` - 看门狗设备路径
  - `watchdog_timeout` - 超时时间（秒）
  - `watchdog_interval` - 喂狗间隔（秒）

**集成情况**:
- 客户端主循环定期喂狗
- 程序退出时正确清理看门狗资源
- 默认禁用，避免在开发环境意外触发系统重启

### 10. 文档完善

**新增文档**:
- `HEARTBEAT.md` - 心跳机制详细说明
- `EMBEDDED_FEATURES.md` - 嵌入式特性完整文档（470行）
  - 功能概述和核心特性
  - API 接口文档
  - 配置参数详解
  - 使用示例和故障排查
  - 针对不同嵌入式设备的配置建议
- `PROJECT_OVERVIEW.md` - 项目功能说明文档（577行）
  - 系统架构和主体功能
  - 技术亮点分析
  - 应用场景和核心竞争力

## 嵌入式优化效果

**可靠性提升**:
- 心跳/重连机制确保网络中断后自动恢复
- 看门狗防止系统死锁
- 播放器健康检查避免僵尸进程

**资源优化**:
- 资源监控实时评估系统负载
- 自适应策略动态调整缓冲区和比特率
- 低资源占用，适合嵌入式设备

**工程化实践**:
- 统一配置文件管理所有参数
- 分级日志系统便于故障诊断
- 模块化设计，易于维护和扩展

**代码统计**:
- 新增代码：约 2100 行（含注释和文档）
- 核心模块：config (200行), logger (180行), heartbeat (150行), resource_monitor (475行), watchdog (379行)
- 文档：约 1600 行
