# 代码优化记录

本次针对 `test` 分支中的 IPv4 组播音频项目做了功能完善和代码规范整理，主要改动如下。

## 1. 服务端功能完善

- 增加启动参数支持：
  - `-d`：守护进程模式
  - `-m <media_dir>`：指定媒体目录
  - `-g <multicast_group>`：指定组播地址
  - `-p <port>`：指定端口
- 媒体目录不再写死到某一台机器上的绝对路径，启动时会自动解析目录。
- 启动时会扫描频道目录，只加载包含 `mp3` 的有效频道目录。
- 没有音频文件的频道目录会被跳过，避免线程反复打开空路径。
- 服务端发送限速不再写死为 `20000 B/s`，改为在加载频道时解析 MP3 首帧码率，并按频道动态初始化令牌桶速率。
- 增加码率兜底和限幅逻辑，探测失败时使用保守默认值，避免发送速率长期高于播放器实际消费速度导致客户端输出缓冲溢出。

## 2. 客户端功能完善

- 修正音频包头解析偏移，保证 `epoch`、`seq`、`payload_len` 按协议正确读取。
- 增加 `stream_epoch` 处理，服务端循环重播同一个音频文件时，客户端可以正确切换到新一轮流。
- 增加启动参数支持：
  - `-g <multicast_group>`：指定组播地址
  - `-p <port>`：指定端口
  - `-P <player>`：指定播放器命令
  - `-c <channel_id>`：直接指定频道，跳过手动输入
- 客户端退出时会主动结束播放器子进程，避免遗留后台进程。

## 3. Qt 上位机接入

- 新增 `qt_control_client/`，提供基于 Qt5 Widgets 的图形化上位机。
- 上位机通过 TCP 与客户端核心联动，支持 `LIST`、`PLAY <id>`、`STOP`、`QUIT` 四类控制命令。
- 界面内置连接参数输入、节目列表、播放控制按钮和状态日志窗口，便于直接观察 GUI 与客户端核心之间的收发过程。
- 客户端核心增加 TCP 控制监听后，可以形成“服务端 UDP 组播推流 + 客户端核心解码播放 + Qt GUI 远程控制”的完整联调链路。

## 4. 代码规范与健壮性

- 统一补充了参数校验和错误处理。
- 服务端频道加载逻辑重构为单一入口，减少重复扫描和无效内存分配。
- 令牌桶实现修正为“按秒速率补充令牌”，避免之前定时周期和注释语义不一致。
- 清理了未使用的辅助函数和部分冗余逻辑。

## 5. 当前构建验证

- `meduim1/server` 可正常编译
- `client/clinet` 可正常编译
- `qt_control_client/qt_control_client` 可正常编译
